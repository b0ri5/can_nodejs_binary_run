{
    "devDependencies": {
        "@bazel/bazelisk": "latest",
        "@bazel/buildifier": "latest",
        "@bazel/ibazel": "latest",
        "@bazel/typescript": "^2.0.3",
        "typescript": "^4.0.2"
    },
    "scripts": {
        "build": "bazel build //...",
        "test": "bazel test //..."
    }
}
